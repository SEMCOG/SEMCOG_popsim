# Data Layout

This directory is reserved for clearer separation of generated and source data during migration.

Planned conventions:
- `data/raw/`: immutable source data and downloads
- `data/interim/`: intermediate prep outputs
- `data/processed/`: finalized run-ready data products
