--- # PopulationSim refinement input package template

# Algorithm / software settings shared by refinement packages.
INTEGERIZE_WITH_BACKSTOPPED_CONTROLS: true
SUB_BALANCE_WITH_FLOAT_SEED_WEIGHTS: false
GROUP_BY_INCIDENCE_SIGNATURE: true
USE_SIMUL_INTEGERIZER: true
USE_CVXPY: false
max_expansion_factor: 30

# These fields are rewritten by the refinement generator for the selected run.
geographies: [REGION, SAMPLEGEO, TAZ]
seed_geography: SAMPLEGEO
data_dir: data
input_table_list: []
household_weight_col: WGTP
household_id_col: hh_id
total_hh_control: num_hh
control_file_name: controls.csv

output_tables:
  action: include
  tables: []

output_synthetic_population:
  household_id: household_id
  households:
    filename: synthetic_households.csv
    columns: []
  persons:
    filename: synthetic_persons.csv
    columns: []

models: []
