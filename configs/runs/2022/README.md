# 2022 Run Config

This directory contains the 2022 run settings.

Expected control file:
- `controls.csv`, typically sourced from the prep-generated `SEMCOG_2022_controls.csv` output

A committed 2022 controls file is not yet present in the repository, so this directory intentionally includes only `settings.yaml` for now.
